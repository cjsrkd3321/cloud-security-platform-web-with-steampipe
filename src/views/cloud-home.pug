extends base
include mixins/row

block content
    table
        thead
            each row, index in rows
                if !index
                    each val, key in row
                        th(style="text-align: center;")=key
        each row in rows 
            +row(row)
        else 
            h3 Nothing