mixin row(title, row)
    tr(style="background-color: rgba(0, 0, 0, 0.2);")
        each val, key in row
            if key != "id"
                td=val
        td
            a(href=`${title}/excepts/${val}`) 
                button(style="padding: 5px;") Except